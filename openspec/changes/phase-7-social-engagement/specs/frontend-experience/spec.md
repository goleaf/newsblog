## ADDED Requirements

### Requirement: Share & Follow UI + Activity Feed
The UI MUST provide share buttons, a follow/unfollow button with state, and a feed view for recent activities.

#### Scenario: Follow button toggles
- WHEN clicking follow
- THEN state toggles to following and counts update.

