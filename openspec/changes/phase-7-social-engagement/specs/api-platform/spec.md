## ADDED Requirements

### Requirement: Follow Endpoints & Share Tracking
Endpoints MUST allow follow/unfollow and listing followers/following; share tracking MUST increment counters per platform.

#### Scenario: Share count increments
- WHEN invoking a share event
- THEN the corresponding counter for the platform increments.

