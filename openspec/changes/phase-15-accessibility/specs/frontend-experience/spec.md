## ADDED Requirements

### Requirement: Accessibility & Responsive UI
Pages MUST render responsively and support keyboard navigation with visible focus and ARIA semantics; images MUST include alt text.

#### Scenario: Keyboard navigation
- WHEN tabbing through a page
- THEN focus indicators are visible and skip links allow bypassing navigation.

