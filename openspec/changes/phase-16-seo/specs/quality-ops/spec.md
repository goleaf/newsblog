## ADDED Requirements

### Requirement: Sitemaps & Meta Tags
The platform MUST expose a sitemap endpoint and render correct meta/OG/Twitter tags based on content.

#### Scenario: Sitemap includes latest published posts
- WHEN generating the sitemap
- THEN published posts and active categories are listed with lastmod timestamps.

