# Change: Phase 4 – Comments & Moderation

## Why
Implement minimal-first commenting with threading and reactions, plus a basic moderation workflow including auto-moderation hooks.

## What Changes
- Specs for CommentController + requests, threaded views, reaction toggles, AutoModerationService, moderation queue UI.

## Acceptance Criteria
- `openspec validate phase-4-comments-moderation --strict` passes.

