## ADDED Requirements

### Requirement: Comment UI Components
The UI MUST include a threaded list, a comment form with validation feedback, and reply forms that respect keyboard accessibility.

#### Scenario: Submit and render new comment
- WHEN submitting a valid comment
- THEN it appears in the list without duplicate submissions and the form resets.

