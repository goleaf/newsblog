## ADDED Requirements

### Requirement: Notification Preferences & Classes
Users MUST have notification preferences (email/in-app/frequency). Notification classes MUST exist for replies, followers, author articles, reactions, and mentions.

#### Scenario: Mark notification read
- WHEN marking a notification as read
- THEN it no longer appears as unread and timestamps are updated.

