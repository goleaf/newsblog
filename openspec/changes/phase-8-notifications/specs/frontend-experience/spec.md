## ADDED Requirements

### Requirement: Notification UI
The UI MUST include a notification dropdown with unread indicator, a list page, and a preferences page.

#### Scenario: Unread indicator updates
- WHEN viewing the dropdown after receiving notifications
- THEN unread count appears and decreases after marking read.

