## Phase 6 – Bookmarks & Reading Lists Checklist

- [x] BookmarkController: toggle/list/remove/mark read-unread/add notes (existing)
- [x] ReadingList API: CRUD + add/remove/reorder + show
  - Implemented endpoints under /api/v1/reading-lists*
- [x] ReadingList sharing: generate/revoke share tokens + public access
  - POST/DELETE /api/v1/reading-lists/{id}/share, GET /api/v1/reading-lists/shared/{token}
- [ ] Views: drag-and-drop UI enhancements for lists (web)
