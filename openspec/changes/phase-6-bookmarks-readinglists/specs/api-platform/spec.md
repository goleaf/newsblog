## ADDED Requirements

### Requirement: Bookmark & Reading List Endpoints
Endpoints MUST support toggling bookmarks, listing/removing, and reading list CRUD + add/remove/reorder + sharing via tokens.

#### Scenario: Reorder list items
- WHEN reordering items
- THEN the new order persists and is reflected on fetch.

