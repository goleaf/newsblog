## 2025-11-13

- [ ] Review current `resources/views/layouts` usage to confirm single-layout plan.
- [ ] Audit `/post/{slug}` rendering (`resources/views`) for design inconsistencies.
- [ ] Catalogue fixes required to replace Bootstrap remnants with Tailwind utilities.
- [ ] Implement prioritized layout updates for `post` detail view.
- [ ] Backfill or update PHPUnit coverage for affected controllers/routes.
- [ ] Run focused tests and formatting checks (`vendor/bin/pint`, `npm run build`, `php artisan test`).

